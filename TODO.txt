1. Window and tab view management
- Expanding and collapsing the entire window view (bars with headings: Current Window, Window 1, ...)
- Select multiple tabs by clicking on the first one and while holding Shift key on the last one to select the range
- Undo operations
- After tab operation refresh the view

2. Tab and window operations
- Faster removal of duplicate tabs
- Faster tab sorting
- Merging all open windows

4. Sorting and filtering
- Alphabetical sorting, ascending/descending
- Sorting by domain, ascending/descending
- Sorting by the number of open tabs for a given website, including subdomains (pl.wikipedia.org and en.wikipedia.org treated as wikipedia.org), ascending/descending

5. Closed tabs and windows
- Add a 'Closed tabs and windows' section
- In the window and tab view, add a notification bubble about closing a given tab

6. Snapshots
- Create and save snapshots of open windows
- Quickly reopen saved snapshots
- Automatically save snapshots

7. Fixes
- Fix move modal so you don't have to click on the radio button, but the whole window card as in the window manager